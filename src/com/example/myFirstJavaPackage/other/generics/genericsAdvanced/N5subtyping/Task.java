package com.example.myFirstJavaPackage.other.generics.genericsAdvanced.N5subtyping;
/*

public class Task {
    public static void main(String[] args) {
        Info<Integer> integerInfo = new Info<>(35);
        Info<String> stringInfo = new Info<>("Thirty-five");
        Info<Car> carInfo = new Info<>(new Car());

        // We can create Info of every type.
        // Task: we need our T parameter be only Number
    }
}

class Info <T> {
    private T value;
    public Info(T value) {
        this.value = value;
    }
    @Override
    public String toString() {
        return "{[ " + value + " ]}";
    }

    public T getValue() {
        return this.value;
    }
}

class Car{}

*/